import * as actionTypes from './action-types';

export const namespace = 'vault-items';

export const GET_VAULT_ITEM = `${namespace}/${actionTypes.GET_VAULT_ITEM}`;
export const GET_VAULT_ITEMS = `${namespace}/${actionTypes.GET_VAULT_ITEMS}`;
export const CREATE_VAULT_ITEM = `${namespace}/${actionTypes.CREATE_VAULT_ITEM}`;
export const SET_CURRENT_VAULT_ITEM = `${namespace}/${actionTypes.SET_CURRENT_VAULT_ITEM}`;
