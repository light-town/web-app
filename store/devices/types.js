import * as actionsTypes from './action-types';

export const namespace = 'devices';

export const INIT = `${namespace}/${actionsTypes.INIT}`;
export const REGIESTER_DEVICE = `${namespace}/${actionsTypes.REGIESTER_DEVICE}`;
